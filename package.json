{
  "name": "ai-campus-assistant-root",
  "private": true,
  "version": "1.0.0",
  "description": "Root-level dev script runner for the AI Campus Assistant backend and frontend.",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend/student-records-portal-main\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
